(function ($, Drupal, once) {
  'use strict';
  Drupal.behaviors.gradGrowFilters = {
    attach: function (context, settings) {
      // const competencyFilter = $('.js-form-item-competency', context);
      // competencyFilter.wrap('<fieldset>');
      //
      // const legend = competencyFilter.find('label[for="edit-competency"]');
      // legend.replaceWith($('<legend>' + legend.text() + '</legend>'));
    },
  };

})(jQuery, Drupal, once);
